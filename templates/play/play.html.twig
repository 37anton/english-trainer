{% extends 'base.html.twig' %}

{% block title %}Hello PlayController!
{% endblock %}

{% block body %}

	<button id="startGameButton" type="button" class="bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow">Démarrer la partie</button>

	<div id="wordToTranslate"></div>

	{{ form_start(form) }}
	{{ form_row(form.translatedWord) }}
	<button id="verifyButton" type="submit" class="bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow">Vérifier</button>
	{{ form_end(form) }}


{% endblock %}
